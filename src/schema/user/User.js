const User = {
  payments: ({ id }, args, { db }, info) => {
    // query db by id of user
  },
  expenses: ({ id }, args, { db }, info) => {
    // query db by id of user
  },
};

module.exports = User;
