const User = {
  //   posts(parent, args, { db }, info) {
  //     return db.posts.filter(({ author }) => author === parent.id);
  //   },
  //   comments(parent, args, ctx, info) {
  //     return db.comments.filter(({ author }) => author === parent.id);
  //   },
};

module.exports = User;
